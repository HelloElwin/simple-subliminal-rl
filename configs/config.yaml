defaults:
  - model: mlp
  - env: default
  - training: default
  - reward: trajectory
  - experiment: default
  - _self_

hydra:
  run:
    dir: exp/${now:%Y%m%d_%H%M%S}
  sweep:
    dir: exp/sweep/${now:%Y%m%d_%H%M%S}
    subdir: ${hydra.job.num}
  job:
    chdir: true
